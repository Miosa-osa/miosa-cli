# MIOSA CLI 1.1.0

## Added

- Added the AWS cloud account, region, pool, and preflight command group.
- Added current sandbox creation resource controls and template lifecycle commands.

## Fixed

- All HTTP and WebSocket paths now derive the runtime User-Agent from `package.json` instead of sending the stale `@miosa/cli/0.1.0` identifier.
